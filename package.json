{
  "name": "app",
  "version": "1.0.0",
  "scripts": {
    "predev": "node utils/env --dev",
    "prestart": "node utils/env --prod",
    "dev": "nodemon --delay 5s index.js",
    "start": "node index.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongodb": "^4.13.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.20"
  }
}